<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_play_program_item"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center_vertical"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/tv_play_serviceid"
        style="@style/Medium"
        android:layout_width="5dip"
        android:layout_height="30dip"
        android:gravity="center_vertical|left"
        android:textColor="@drawable/program_text_color_selector"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/tv_play_program_number"
        style="@style/Medium"
        android:layout_width="80dip"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|left"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:textColor="@drawable/program_text_color_selector" />

    <TextView
        android:id="@+id/tv_play_program_name"
        style="@style/Medium"
        android:layout_width="200dip"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dip"
        android:ellipsize="marquee"
        android:focusableInTouchMode="true"
        android:gravity="center_vertical|left"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:textColor="@drawable/program_text_color_selector" />

    <ImageView
        android:id="@+id/iv_fav_tag"
        android:layout_width="30dip"
        android:layout_height="30dip"
        android:src="@drawable/favorite"
        android:visibility="invisible" />

    <ImageView
        android:id="@+id/iv_lock_tag"
        android:layout_width="30dip"
        android:layout_height="30dip"
        android:src="@drawable/lock"
        android:visibility="invisible" />

</LinearLayout>